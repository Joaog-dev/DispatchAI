# Contracts package

Este pacote define schemas Zod e tipos Typescript para contratos entre front e back.

Uso:
- import { DeliverySchema } from "@dispatchai/contracts"
- validar payloads no front e no gateway IA

Arquivos em `src/` contem somente definicoes (sem runtime extra).
